version: '3.8'

services:
  aips:
    build: .
    container_name: aips-monitor
    volumes:
      # Mount your local data folder to /data in container
      - ./data:/data
    environment:
      # You can also override env vars here if needed
      - TZ=UTC
    restart: unless-stopped
